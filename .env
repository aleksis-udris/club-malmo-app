# Frontend (Vite). The app works fully offline with the in-browser court hub.
# To consume the real backend instead, point this at the NestJS server and
# adapt src/pairing/courtHub.ts to call it (REST + Socket.IO).
VITE_API_BASE_URL=http://localhost:3000/api/v1

SPORTRADAR_API_KEY=cpFLRlHuUSKskWx4HmFd6sMPMCogD9iYr3wMSl3N
SPORTRADAR_ENABLED=true
SPORTRADAR_BASE_URL=https://api.sportradar.com/squash/trial/v1/en